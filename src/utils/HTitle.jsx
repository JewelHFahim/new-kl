
const HTitle = ({children}) => {

    return (
        <div>
            <h1 className="text-[#8F8DF1] text-[20px] font-[800] uppercase text-center my-[10px] font-poppins">{children}</h1>
        </div>
    );
};

export default HTitle;